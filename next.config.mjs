/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['cambridgeonline.uz','images.unsplash.com','mdbcdn.b-cdn.net'],
    },
  };
  
  export default nextConfig;
  